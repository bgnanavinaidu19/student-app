services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile 
    ports:
      - "5051:5050"
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"