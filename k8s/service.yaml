apiVersion: v1
kind: Service
metadata:
  name: study-planner-service
spec:
  type: NodePort
  selector:
    app: student-app
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 5050
      nodePort: 30007